; a. The 2 procedures generate the same output list. The produced list of fig. 2.16 is (1 3 5 7 9 11)

; b. The second procedure is faster because it only uses cons(O(1)) at each node and visits each node once.
; The time complexity is O(n) if the tree has n nodes